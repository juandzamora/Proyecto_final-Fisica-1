#pragma once

#include <raylib.h>

namespace raylib_utils
{
	//organiza la posición de un vector dependiendo del ancho actual de la pantalla
	void fixPosition(Vector2 & vector)
	{
		int ancho_pantalla = GetScreenWidth();
		int alto_pantalla = GetScreenHeight();

		
	}
}